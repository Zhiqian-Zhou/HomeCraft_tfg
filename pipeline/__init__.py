"""HomeCraft v2 pipeline package.

Top-level layout:
    pipeline/skills/    Skill library (one Python module per skill).
    pipeline/...        Future: agents, architect, composer (Phase later).
"""
